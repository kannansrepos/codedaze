export type PostIndex = {
  postId: string;
};

export type Slag = {
  SEO_Keywords_List: string;
  title: string;
  date: string;
  language: string;
  meta_description: string;
  readTime: string;
  subtitle: string;
};
